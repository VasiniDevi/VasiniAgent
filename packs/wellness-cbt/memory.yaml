schema_version: "1.0"
short_term:
  enabled: true
  ttl: "24h"
  max_entries: 200

episodic:
  enabled: true
  confidence_threshold: 0.6
  retrieval_top_k: 10
  similarity_threshold: 0.65
  source_required: false

factual:
  enabled: true

cross_session:
  enabled: true
  merge_strategy: latest
